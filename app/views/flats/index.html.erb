<div class="container mt-5">
  <h1>Flats</h1>
  <div class="cards">
    <% @flats.each do |flat| %>
      <div class="card-trip">
        <%= image_tag flat['imageUrl'], alttext: 'flat picture' %>
        <div class="card-trip-infos">
          <div>
            <h2><%= flat['name'] %></h2>
          </div>
          <h2 class="card-trip-pricing">€<%= flat['price'] %></h2>
        </div>
        <%= link_to "", flat_path(flat['id']), class: 'card-link' %>
      </div>
    <% end %>
  </div>
</div>
